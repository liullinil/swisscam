{
  "name": "swisscam",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Редактор и симулятор управляющих программ для станков продольного точения (swiss-type)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2"
  }
}
